Fields:
  - Name: "DealReference"
    Description: "The customer contract deal reference"
    Validation: "combine([length(3), allCaps, startWith('x')]);"
  - Name: "BookingReference"
    Description: "The captured date"
    Validation: combine([length(5)]);